spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/secureauth}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration
